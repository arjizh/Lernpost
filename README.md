# Lernpost

React + Supabase Setup für Gymicards Lernpost (Login, Dashboard, Stripe Checkout Edge Function).

## Entwicklung starten

```bash
npm install
npm run dev
```

Um die Supabase Edge Function lokal zu testen:

```bash
supabase functions serve --env-file .env.local create-checkout
```
